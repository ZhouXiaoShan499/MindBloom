# MindBloom · 环境智能学习灯 · 对内技术规格

**文档版本**：V2.0  
**更新日期**：2026-05-23  
**适用对象**：硬件、固件、算法、测试、供应链、评审答辩（含能力边界与未承诺项）

---

## 1. 产品定义（内部）

**形态**：桌面台灯 + 24GHz FMCW 雷达模组（HLK-LD2420B 或经实测等效方案）+ ESP32-S3 主控 + 主照明光路 + 可选 RGB 氛围。

**对外叙事锚点**：AI 自适应学习、在位检测、低活动累计、慢变光提示、手动呼吸光、一键重置优先、隐私本地化。

**V2 升级摘要**：
| 维度 | V1（旧） | V2（当前） |
|------|---------|-----------|
| 产品定位 | 学习情绪显影灯 | 环境智能学习灯 |
| 阈值策略 | 硬编码固定阈值（motion < 0.3） | AI 自适应基线（指数移动平均） |
| 久坐窗口 | 固定 25~35 min | 动态学习（10~60 min，基于用户历史） |
| 用户模型 | 无 | 本地 UserProfile（JSON 持久化） |
| 数据存储 | 无 | 仅存本地（不上云，不联网） |

**合规**：Wellness 产品；禁止对外宣称医疗诊断、治疗、稳定心率/HRV 改善等无验证结论。

---

## 2. 感知层（雷达）— 能力与边界

### 2.1 模组与接口

- **首选 BOM**：HLK-LD2420B（24GHz），UART 默认 115200（以模组手册为准）。  
- **输出利用**：存在/离开、距离门限内目标、能量或微动等级（以固件解析字段为准）。

### 2.2 可稳定用于量产的信号

| 信号 | 用途 |
|------|------|
| 在位 / 离开 | 人来亮、离开延时熄灯、离开兜底逻辑 |
| 活动强弱代理 | 滑动窗口统计：事件率、能量分位数、距离稳定性等 |
| 低活动累计 | 久坐提示触发与清零条件之一 |

### 2.3 研发项 / 不对外承诺项

- **呼吸节律弱特征**：在固定安装几何、单人、SNR 足够时，可尝试 FFT/自相关等提取周期成分，用于 **可选** 自动安抚的 **保守触发**；**不得**对外承诺医疗级呼吸频率或呼吸诊断。  
- **心率（HR）/ HRV**：**单灯 + 该档位雷达** 一般 **不足以**作为对外产品指标；若需生理指标，需 **PPG 或** 经临床级验证的雷达方案，属 **另一条硬件 SKU**。  
- **「学习姿态」「学习类动作」「纸质/手机/娱乐区分」**：雷达 **不可单独实现**；若产品需要，须 **第二传感器**（如桌垫振动、用户自愿手机侧信道等），并 **独立 SKU 或可选配件**，不得写进雷达主叙事。

---

## 3. 推理与状态机（内部）

### 3.1 推荐架构

- **V1（MVP）**：状态机 + 固定阈值 + 滞回（hysteresis）
- **V2（当前）**：状态机 + **UserProfile 自适应学习** + 动态阈值 + 滞回
- **V3（可选）**：TensorFlow Lite Micro，输入为手工特征向量，输出专注/思考/分心/疲劳四分类

### 3.2 状态（内部命名）

`IDLE_OFF`、`BASE_STUDY`、`LOW_ACTIVITY_NUDGE`、`BREATHE_MANUAL`、`LEAVE_DIM`

**优先级（从高到低）**：用户重置 / 手动退出呼吸 → 离开熄灯逻辑 → 呼吸模式内规则 → 久坐缓暗累计

### 3.3 UserProfile 画像系统（V2 新增）

| 画像字段 | 类型 | 说明 |
|---------|------|------|
| `motion_baseline` | float (0.05~0.50) | 用户个人的"安静"基线，EMA 实时更新 |
| `motion_active_threshold` | float (0.25~0.80) | 活跃阈值 = baseline + 0.20 |
| `typical_focus_duration` | float (秒) | 历史专注时长的中位数 |
| `recent_focus_sessions` | list[float] | 最近 20 次专注时长记录 |
| `preferred_color_temp` | float (0~1) | 用户偏好的色温（预留，V2 暂不启用） |
| `typical_session_length` | float (秒) | 典型单次学习时长 |
| `total_sessions` | int | 总学习次数 |
| `adaptation_count` | int | 自适应调整次数 |

**学习算法**：指数移动平均（EMA） + 中位数统计
- 不需要神经网络，ESP32-S3 单核即可运行
- 画像数据序列化为 JSON，仅存本地 Flash

### 3.4 久坐提示（内部参数，需标定）

- **启动窗口**：动态（初始 25min，AI 调整至 10~60min）
- **变暗策略**：例如每 15 min 亮度 −3%，**下限** 60%～70%（可配置）；**全周期最大下降速率** 封顶
- **清除条件**：显著活动 bump 超阈；单击重置；进入手动呼吸（按产品定义是否暂停累计）

### 3.5 放松呼吸光（内部）

- **默认路径**：长按 ~2 s 进入；短按或重置退出（与对外说明书一致）
- **波形**：正弦或分段线性；周期 10～12 s；幅度 ±3%～±10% 以 **主观舒适 + 无可见频闪** 实测为准
- **自动触发（若存在）**：**默认关闭**；仅当多特征保守满足且用户曾开启选项；**必须**可被重置 **<100 ms** 中断

### 3.6 禁止对外输出的内部标签

避免将 UI 文案写为「专注 / 焦虑 / 拖延确诊」；对外统一为 **光语模式**（见对外文档）。

---

## 4. 光语引擎（内部）

- **渐变**：所有亮度变化设 **最大斜率**（lux/s 或 PWM duty/s），避免跳变与眩光
- **色温与 RGB**：COB 冷白与暖黄双通道独立 PWM 调光，实现无级色温调节；若存在 RGB 氛围通道，与 **主照明通道** 解耦；双通道 COB 均可达到高显指，需 **实测 Ra** 报告支撑
- **频闪**：需过粗测（手机快门 / 示波器）与目标市场相关参考限值（内部目标，非医疗）

---

## 5. 交互（内部 BOM 建议）

| 方案 | 优点 | 缺点 |
|------|------|------|
| **旋钮 + 按键** | 低成本、低误触、易解释「重置」 | 外观需开孔 |
| **电容触控环** | 一体化好看 | 手势栈复杂、量产校准与误触成本高 |

**MVP 内部建议**：旋钮调亮 + 单键重置/长按呼吸；触控环放 **V2.1**。

**手势互斥表（若上触控环）**：须单独文档维护：单击 / 双击 / 长按 / 连击 / 画圈的时间窗与互斥，避免「双击既开关又回顾」类冲突。

---

## 6. 硬件 BOM（内部草案）

| 器件 | 说明 |
|------|------|
| MCU | ESP32-S3（Flash/PSRAM 按是否上 TFLite 与日志定） |
| 雷达 | HLK-LD2420B |
| 主光 | COB 冷白 + COB 暖黄双通道 + 恒流/PWM（双通道独立调光，推荐与 RGB 分离） |
| 辅光（可选） | 少量 WS2812 或 RGB 模组 |
| 电源 | 5V/2A USB-C，按峰值电流留裕量 |
| 结构 | 雷达视场与安装角在《机械设计说明》中冻结 |

**DevKit**：ESP32-S3-DevKitC-1 仅作开发；量产 **自制 PCB**。

---

## 7. 软件架构（内部模块）

1. **RadarDriver**：UART 帧解析、校验、丢包恢复
2. **FeatureExtractor**：滤波、滑动窗口、特征向量
3. **UserProfile**（V2 新增）：自适应基线、专注时长学习、本地画像持久化
4. **PolicyEngine**：状态机、动态阈值、滞回、档位配置
5. **LightEngine**：曲线、呼吸发生器、渐变限速
6. **Storage**：V2 存配置 + 用户画像（JSON）；历史光轨为 V3（仅存摘要特征，不存原始 IQ）
7. **OTA（可选）**：默认编译开关关闭；若开启，需 **安全启动 / 签名** 与 **隐私数据不上传** 设计说明

---

## 8. 功能分阶段（内部 Roadmap）

| 阶段 | 范围 |
|------|------|
| **MVP** | 在位亮灭、久坐缓暗、活动恢复、手动呼吸、重置、灵敏度档位、白光优先 |
| **V2（当前）** | **AI 自适应基线 + 专注时长学习 + 本地画像持久化** |
| **V2.1** | 触控环调光调色、画圈念头（若误触率达标）、可选自动安抚（默认关） |
| **V3** | TFLite 时序分类（专注/思考/分心/疲劳）、结束仪式光轨、7 日摘要回顾 |

---

## 9. 风险登记与对策（内部）

| 风险 | 等级 | 对策 |
|------|------|------|
| 深度思考静止触发久坐 | 中 | 自适应基线 + 慢速 + 下限 + 档位 + 重置清零 |
| 旁人走过触发在位 | 中 | 距离门、安装指南、指向角、滞回 |
| 自动安抚误判 | 高 | 默认关；保守阈值；重置优先 |
| 学习数据安全 | 中 | 仅存本地 JSON，不上云；用户可手动删除 |
| 自适应基线漂移 | 低 | 保底范围 0.05~0.50；learning_rate 可配置 |
| RGB 与 Ra 宣称冲突 | 中 | 白光独立通道；对外只报白光 Ra |
| 手势冲突 | 中 | 状态机表 + 超时 + 用户测试 |
| OTA 侵蚀「无联网」叙事 | 中 | 对外分 SKU 或默认无 Wi‑Fi 模组 |

---

## 10. 测试清单（内部验收）

- 静坐阅读、深度发呆、玩手机、起身走动、多人穿行 —— 各 30 min 日志与主观表
- 自适应基线漂移测试：连续模拟不同 motion_level，验证收敛行为
- 专注时长学习验证：模拟多次短/中/长专注，验证 nudge_window 动态调整
- 画像持久化测试：关机重启后画像是否正确恢复
- 重置延迟、渐变斜率、呼吸幅度 AB 测
- 断电记忆（配置项 + 画像）、长时间运行温升
- 说明书可用性测试：不看论坛能否独立完成「关久坐提示」

---

## 11. 与《对外产品说明》的同步规则

- **对外未写明的能力** = **未承诺**；研发可在内部 Roadmap 存在，但不得用于营销默认包
- 任何涉及 **生理指标、情绪诊断、学习类型识别** 的对外文案，须 **工程 + 法务/合规** 双签
- V2 对外关键词：AI 自适应、隐私本地化、动态学习、无感陪伴

---

**— 对内文档结束 —**
